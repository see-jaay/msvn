// boundaries test for mouse overlays + component overlap and layering

class Boundary {
  //origin, width, height, shape
  constructor(c,o,l,r,t,b) {
    this.component = c;
    this.origin =
    // groups of boundaries to compose a composite 
    this.composite = ['c/2.5,2/'];

  }
}
